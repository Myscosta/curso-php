Criamos um pasta com o nome "01_exemplo_de_login" dentro dela criamos arquivos como:
--formulario.php: e dentro dela iniciamos com o modelo de inicio da linguagem de marcação do hltml, em seguida criamos um modelo de fromulário:
--com os campos de E-MAIL e SENHA  e os buttons de ACESSAR E LIMPAR 
 --criamos a validação de login: usando uma variavel para validar um email e senha padrão e nela usamos o POST para envio de formulario para a proxima pagina.
  entao caso esteja de com as infromações correta seguirá para a proxima pagina que é "tela_administrativa" e caso de erro usamos o GET para a exibição do erro na barra de pesquisa.
